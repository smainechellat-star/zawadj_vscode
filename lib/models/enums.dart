// Gender Enum
enum Gender { male, female }

// Education Level
enum EducationLevel { primary, middle, secondary, university }

// Marital Status
enum MaritalStatus { single, married, divorced, widowed }

// Employment Status
enum EmploymentStatus {
  governmentEmployee,
  privateEmployee,
  selfEmployed,
  unemployed,
  retired,
  noJob,
}

// Housing Type
enum HousingType { separateHouse, withParentsOnly, familySharedHouse }

// Account Status
enum AccountStatus { available, inSeriousProcess, married, withdrawn }

// Height Range
enum HeightRange { short, medium, tall }

// Skin Color
enum SkinColor { fair, wheatish, brown, dark, noPreference }

// Body Type
enum BodyType { slim, average, heavyset, athletic, noPreference }

// Glasses Status
enum GlassesStatus { yes, no, sometimes, doesntMatter }

// Habit Status
enum HabitStatus { yes, no, noMatter }

// Prayer Status
enum PrayerStatus { regular, irregular, doesntMatter }

// Children Age Range
enum ChildrenAgeRange { above20, from10to20, below10 }

// Children Preference
enum ChildrenPreference { yes, no, doesntMatter }

// Glasses Preference
enum GlassesPreference { yes, no, doesntMatter }

// Appearance Filter
enum AppearanceFilter { veryImportant, important, doesntMatter }

// Marriage Intent
enum MarriageIntent { first, second, third, fourth }

// Language
enum AppLanguage { english, arabic }
